# 展場中控系統 — 環境變數範本
# 用法：複製本檔為 .env 後填值（.env 已在 .gitignore，不會被 commit）
#
# cp .env.example .env

# === LLM ===
# 取得 Claude API key：https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# 強制覆蓋 chat.json 的 provider（緊急切換用，可留空）
# LLM_PROVIDER=ollama

# === 雲端模式 ===
# CLOUD=1
# BRIDGE_SECRET=your-secret-here

# === 服務 port（可留空，預設 3000）===
# PORT=3000

# === Phase 5+ 將會用到（目前可留空）===
# DEEPGRAM_API_KEY=
# C230_RTSP_URL=rtsp://user:pass@192.168.x.x:554/stream1
